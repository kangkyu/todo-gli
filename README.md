# todo_gli

```
bundle install
bundle exec bin/todo new
bundle exec bin/todo done
bundle exec bin/todo list
```

```sh
$ bundle exec bin/todo -f ~/todo.txt new -f --priority abc "A new task" "Another task"
Global:
-f - /Users/ugp/todo.txt
Command:
-f - true
--priority - abc
args - A new task,Another task
```
